# ============================================
# Core DICT - Real Mode Configuration
# ============================================
# Copy this to .env and adjust values for your environment

# -------------------- Application Mode --------------------
# Set to "false" to enable Real Mode (requires all infrastructure)
# Set to "true" to enable Mock Mode (no dependencies needed)
CORE_DICT_USE_MOCK_MODE=false

# -------------------- gRPC Server --------------------
GRPC_PORT=9090
LOG_LEVEL=info

# -------------------- PostgreSQL Database --------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lbpay_core_dict
DB_USER=dict_app
DB_PASSWORD=dict_password
DB_SCHEMA=core_dict

# Database Timeouts
DATABASE_TIMEOUT=10s

# -------------------- Redis Cache --------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Redis Timeouts
REDIS_TIMEOUT=5s

# -------------------- Apache Pulsar (Optional) --------------------
PULSAR_URL=pulsar://localhost:6650

# -------------------- Connect Service (Optional) --------------------
# Enable Connect service integration (conn-dict gRPC client)
CONNECT_ENABLED=false
CONNECT_URL=localhost:9092
CONNECT_TIMEOUT=10s

# -------------------- Participant ISPB --------------------
# LBPay ISPB (Identificador do Participante no DICT)
PARTICIPANT_ISPB=12345678

# -------------------- Quick Start with Docker Compose --------------------
# To start all infrastructure locally, run:
#   docker-compose up -d
#
# This will start:
#   - PostgreSQL on port 5432
#   - Redis on port 6379
#   - Pulsar on port 6650 (optional)
#
# Then set CORE_DICT_USE_MOCK_MODE=false and run:
#   go run cmd/grpc/main.go cmd/grpc/real_handler_init.go
